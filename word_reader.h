#ifndef COUNTS_H
#define COUNTS_H

// Declare global variables for total count and character count
extern int TOT_COUNT;
extern int CHAR_COUNT;

// Function prototypes
void word_count();
void addCharCount(int n);

#endif // COUNTS_H
